/* function velocidadeMedia(distPercorrida, tempo) {
    vM = distPercorrida / tempo * 3.6;
    console.log(`Sua velocidade média é de ${vM.toFixed(1)} km/h`);
}

velocidadeMedia(1000, 300); */

const distPercorrida = 1000, tempo = 300;

const velocidadeMedia = distPercorrida / tempo * 3.6;

console.log(`Sua velocidade média é de ${velocidadeMedia} km/h`);


